# 2026-05-16

## 根本教训（经过9轮修复总结）

### 为什么不断出问题
1. **正则删除CSS → 留孤儿代码**：`@keyframes` 的 `}` 和内部行无法完整匹配，残留的 `}` 破坏CSS平衡
2. **类名不统一**：energy 用 `title-bar`/`btn`，其他页用 `title-box`/`common-btn`，共享CSS覆盖不全
3. **CSS变量被删但引用还在**：页面引用 `var(--sidebar-width)` 等变量，但定义被删除→undefined
4. **首页链接到旧文件**：index.html→park_overview.html(双标题) vs 底部tab→park_economy.html(单标题)

### 第9轮修复（最终轮）
- 所有7页CSS括号平衡(depth=0) ✅
- energy按钮改为 `class="common-btn"` + `class="back-btn"` ✅
- energy补充完整 `.title-bar h1` 和 `.title-bar .time` CSS ✅
- park_common.css 补全5个缺失CSS变量(--text-primary, --text-tertiary, --success-color, --sidebar-width, --split-color) ✅
- 所有页面CSS变量全部覆盖 ✅
- 企业/服务不再加载AMap脚本 ✅
- index.html 链接改为 park_economy.html ✅
- 所有页面引用 park_common.css ✅
## 修复 park_*.html 页面双重切换面板问题

### 问题
7个园区子页面（park_sandbox/economy/land/enterprise/service/energy/safety.html）存在多重注入的标题栏（ph-bar、park-hdr）和切换面板（park-tabs），与原有的动态圆形 .bottom-tabs 冲突，导致：
- 双重/三重标题栏
- 双重切换面板（浮动 park-tabs + 圆形 bottom-tabs）

### 修复方案

#### overview-based 页面（economy/enterprise/service）
- **源文件**: park_overview_source.html
- **方法**: Python 脚本清理所有注入代码，恢复 screen-container > title-box 结构
- **关键改动**:
  - 移除所有 ph-bar/park-hdr/park-tabs CSS 和 HTML
  - 恢复 `<div class="screen-container">` 包裹 + `<div class="title-box">` 标题栏
  - bottom-tabs 使用 `<a>` 链接指向其他 park_*.html（非 in-page panel 切换）
  - enterprise 页自动展开 entPanel + renderEnterpriseList()
  - service 页自动展开 servicePanel + initServiceDashboard()

#### 非 overview 页面（sandbox/land/safety）
- 移除底部注入的 park-hdr + park-tabs
- 添加圆形 .bottom-tabs CSS + 7个 tab-btn `<a>` 链接
- 保留原有 title-box 标题栏

#### energy 页面
- 从 park_energy_source.html 重新生成
- 移除 ph-bar 注入代码
- 更新 bottom-tabs 为完整 7 个 tab
- 保持全屏 ECharts 地图布局

### 最终状态
所有 7 个页面：
- park-hdr: 0, park-tabs: 0, ph-bar: 0, ph-wrap: 0
- bottom-tabs HTML: 各 1 组（7 个圆形 tab-btn 按钮）
- 每个页面正确的 active tab 标记
- 圆形按钮使用 `<a>` 链接实现页面间导航（非 JS panel 切换）

### 追加修复 (18:44~18:48)
根据截图发现并修复：
1. **park_sandbox/land/safety** - bottom-tabs CSS 裸露在 `<style>` 标签外，导致页面顶部显示CSS代码
   → 包裹在 `<style>...</style>` 内
2. **park_energy.html** - 完全没有CSS样式，白底、文字链接
   → 从零重建，补全深色主题、悬浮面板、圆形按钮等全部样式
3. **park_economy/enterprise/service** - AMap安全配置脚本正常，无问题

### 第三轮修复 (19:03) - 对比统一
根据用户反馈逐页对比，发现并修复：

| 问题 | 根因 | 修复 |
|------|------|------|
| 企业/服务看不到切换面板 | entPanel z-index:200 > bottom-tabs:110 | 降为z-index:50 |
| 企业/服务看不到标题 | showPanel()未隐藏loadingOverlay (z-index:9999) | 添加hide逻辑 |
| 能耗无标题 | 全屏地图缺标题栏 | 添加固定标题栏 |
| 经济标题样式不同 | 旧updateClock() null.textContent抛异常阻塞后续脚本 | 替换为空函数 |
