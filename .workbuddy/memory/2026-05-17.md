# 2026-05-17

## 曲线图顶点标签遮挡修复
- **文件**: pages/park_charts.js
- **改动**: `grid.top` 从 `10` → `28`，曲线图顶部数据标签不再被裁切
- **不影响**: 图表容器宽高占比完全不变，仅 ECharts 内部 grid 上边距微调
- **影响范围**: 仅 park_energy.html 使用 trendLine，其它页面不受影响
- **containLabel:true** 保留，自适应补偿标签空间

## 曲线图网格线颜色统一
- yAxis.splitLine.color 设为 T.text (#8ab4d8)，opacity .15，与轴文字同色
- 修复前走 ECharts 默认白色，与暗色主题不协调

## 图表标准文档化
- park_charts.js 顶部新增完整 JSDoc 注释，将曲线图定为 ★图表标准#1
- 涵盖：生成方法签名、视觉规范、曲线样式、数据标签、Grid 自适应间距（含修复记录）、网格线、图例、交互、X 轴
- 后续新增图表类型（饼图、柱状图等）可参照此格式追加

## 能耗排名柱形图优化
- **文件**: pages/park_energy.html, initRank()
- **排序**: 降序排列 + `inverse:true`，最高值在最上面
- **标签遮挡修复**: `grid.right` 从 `6` → `45`，为 `position:'right'` 标签留空间（同曲线图 top:28 思路）
- **单位**: `formatter:'{c} 万元'` 改为完整中文单位
- **网格线**: `containLabel:true` 自适应补偿
- **动态效果**: 
  - `animationDuration:1200` + `animationEasing:'elasticOut'` 弹性入场
  - `animationDelay` 逐条错开
  - `emphasis` hover 光晕放大（shadowBlur:20 + scale:1.08）
- **图例**: 底部 `data:['能耗成本']`，与曲线图保持一致
- **tooltip**: axis 触发 + shadow 指针

## 柱状图标准化入库 ★图表标准#2
- **park_charts.js**: 新增 `ParkCharts.barRank(domId, opts)` 工厂函数
  - opts: { items:[{name,value}], name, unit, colors? }
  - 自动降序排列 + inverse:true → 最高值在最上面
  - 逐条独立颜色（rankColors 9色循环），Y轴企业名与柱体同色
  - series.color: rankColors[0] 供图例Icon取色
- **JSDoc 文档**: 完整记录生成方法、视觉规范、排序规则、柱体样式、数据标签、Grid布局、网格线、坐标轴、图例、交互与动画
- **park_energy.html**: initRank() 从 70 行内联代码精简为 3 行调用
- **图例统一**: 曲线图 & 柱状图均用 icon:'roundRect' + itemWidth:16 + itemHeight:6

## 南丁格尔玫瑰图标准化入库 ★图表标准#3
- **park_charts.js**: 新增 `ParkCharts.pieRose(domId, opts)`
  - opts: { data:[{name,value}], name?, colors? }
  - roseType:'area' 南丁格尔玫瑰图，扇区半径映射数值
  - radius:['22%','62%'] 外径 62% 为标签留空间
  - center:['50%','55%'] 上移为底部图例留空间
  - borderRadius:6 + borderColor:T.bg + shadowBlur:10
  - 9 色循环 palette，逐条独立 itemStyle.color
  - 图例 icon/字号与曲线图柱状图完全一致
- **park_energy.html**: initPie() 精简为 3 行调用
- **全平台三图表统一维度**: 入场动画(curve+bar)、图例样式、标签字号/色、间距设计原则

## 曲线图补入场动画 ★图表标准#1修订
- animationDuration:1200 + animationEasing:'elasticOut' + animationDelay:idx*100
- 与柱状图动画维度对齐
